# Murmur

Parametric thinking indicators for AI products. 24 Metal shader species, one
uniform SwiftUI API, a lab app for designing a configuration by hand, and a
one-tap export that hands the exact configuration to a coding agent.

Work in progress. See SPEC.md for the build contract.
