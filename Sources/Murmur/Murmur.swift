/// Murmur. Parametric thinking indicators: living material, not spinners.
public enum MurmurInfo {
    public static let version = "0.1.0"
    /// Written into every agent export as the dependency line, so a prompt
    /// copied out of the lab points at the real package.
    public static let repositoryURL = "https://github.com/krispuckett/murmur"
}
